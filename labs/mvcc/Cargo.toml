[package]
name = "mvcc"
version = "0.1.0"
edition = "2021"
description = "forge lab 04 — MVCC: versioned rows, snapshots, and the visibility rule (tablespace)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kslab = { path = "../kit" }

[profile.release]
opt-level = 2
debug = false
