[package]
name = "buffer-pool"
version = "0.1.0"
edition = "2021"
description = "forge lab 07 — a buffer pool manager with an LRU-K replacer (tablespace)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kslab = { path = "../kit" }

[profile.release]
opt-level = 2
debug = false
