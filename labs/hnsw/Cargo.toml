[package]
name = "hnsw"
version = "0.1.0"
edition = "2021"
description = "forge lab 06 — a mini-HNSW vector index with an honest cost model (tablespace)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kslab = { path = "../kit" }

[profile.release]
opt-level = 2
debug = false
