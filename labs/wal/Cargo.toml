[package]
name = "wal"
version = "0.1.0"
edition = "2021"
description = "forge lab 03 — a write-ahead log with crash recovery: ordering, checksums, replay (tablespace)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kslab = { path = "../kit" }

[profile.release]
opt-level = 2
debug = false
