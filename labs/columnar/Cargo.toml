[package]
name = "columnar"
version = "0.1.0"
edition = "2021"
description = "forge lab 09 — compressed columns, zone maps, and a vectorized filter+sum (tablespace)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kslab = { path = "../kit" }

[profile.release]
opt-level = 2
debug = false
